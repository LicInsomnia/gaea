server:
  port: 11110
node:
  name: insomnia
spring:
  application:
    name: core_controller
  cloud:
    nacos:
      config:
        server-addr: 172.16.1.102:8848
        name: gaea
        file-extension: yml
  profiles:
    active: ${node.name}