server:
  port: 11112
spring:
  application:
    name: src_session
  profiles:
    active: sparrow
  cloud:
    nacos:
      config:
        server-addr: 172.16.1.102:8848
        name: gaea
        file-extension: yml
        group: GAEA_GROUP
        extension-configs:
          - dataId: common_config.yml
            group: GAEA_GROUP
node:
  name: sparrow
logging:
  level:
    root: info
