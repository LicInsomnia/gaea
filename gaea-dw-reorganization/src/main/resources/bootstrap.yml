server:
  port: 11112
node:
  name: insomnia
spring:
  application:
    name: dw_reorganization
  cloud:
    nacos:
      config:
        server-addr: 172.16.1.102:8848
        name: gaea
        file-extension: yml
  profiles:
    active: ${node.name}